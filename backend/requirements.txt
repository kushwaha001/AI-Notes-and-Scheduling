# ── Core API ──────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.8.0

# ── Database ──────────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── HTTP client (for Ollama + service health checks) ──────────
httpx==0.27.2
# faster-whisper's model downloader imports `requests` directly and it isn't
# always pulled transitively — pin it so the Whisper import doesn't fail.
requests==2.32.3

# ── Vector DB client ──────────────────────────────────────────
qdrant-client==1.12.1

# ── Cache ─────────────────────────────────────────────────────
redis==5.2.1

# ── Document processing ───────────────────────────────────────
pypdf==5.1.0
pillow==11.1.0
pytesseract==0.3.13          # requires Tesseract-OCR installed separately
# Renders PDF pages to images for the vision-extraction path (api/ai/render.py).
# Self-contained wheel, no system libs — safe for the air-gapped bundle. Optional:
# if absent, image uploads still use vision; only PDF->image rendering is skipped.
PyMuPDF==1.24.10

# ── Voice transcription (Whisper, CPU-capable) ────────────────
# Runs Whisper locally; falls back to CPU/int8 when there's no GPU, so voice
# notes transcribe even in a plain container or Codespace. Pulls ctranslate2 and
# av (ffmpeg bundled in the wheel) — no extra system packages required.
faster-whisper==1.1.0
