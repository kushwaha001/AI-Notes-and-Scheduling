# ── Core API ──────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.8.0

# ── Database ──────────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── HTTP client (for Ollama + service health checks) ──────────
httpx==0.27.2

# ── Auth (v2 Keycloak / OIDC token validation — needed when AUTH_ENABLED=true) ─
PyJWT==2.13.0
cryptography==49.0.0          # RS256 signature verification for PyJWT

# ── Vector DB client ──────────────────────────────────────────
qdrant-client==1.12.1

# ── Cache ─────────────────────────────────────────────────────
redis==5.2.1

# ── Document processing ───────────────────────────────────────
pypdf==5.1.0
pillow==11.1.0
pytesseract==0.3.13          # requires Tesseract-OCR installed separately
